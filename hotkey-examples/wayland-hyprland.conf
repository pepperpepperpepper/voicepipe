# Add this to ~/.config/hypr/hyprland.conf for Hyprland (Wayland compositor)

# Voicepipe toggle
bind = $mainMod, F5, exec, /home/pepper/.local/bin/voicepipe-fast toggle

# Alternative binding
# bind = $mainMod, R, exec, /home/pepper/.local/bin/voicepipe-fast toggle

# Reload hyprland config with: hyprctl reload