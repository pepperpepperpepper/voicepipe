# Add this to ~/.config/sway/config for Sway (Wayland compositor)

# Voicepipe toggle
bindsym $mod+F5 exec /home/pepper/.local/bin/voicepipe-fast toggle

# Alternative binding
# bindsym $mod+r exec /home/pepper/.local/bin/voicepipe-fast toggle

# Reload sway config with: swaymsg reload