# Example environment variables for voicepipe
#
# NOTE: systemd user services won't read your shell rc files; prefer
# ~/.config/voicepipe/voicepipe.env (managed by `voicepipe setup` / `voicepipe config …`)

# Transcription backend: openai (default) or elevenlabs
VOICEPIPE_TRANSCRIBE_BACKEND=openai

# OpenAI (required when backend=openai)
OPENAI_API_KEY=your-openai-api-key-here

# ElevenLabs (required when backend=elevenlabs)
# ELEVENLABS_API_KEY=your-elevenlabs-api-key-here
# or: XI_API_KEY=your-elevenlabs-api-key-here

# Transcription model:
# - openai: gpt-4o-transcribe (default), whisper-1, ...
# - elevenlabs: scribe_v1 (default), scribe_v1_experimental, ...
# VOICEPIPE_TRANSCRIBE_MODEL=gpt-4o-transcribe

# Typing backend for `--type` (optional override; auto-selects based on platform/session)
# VOICEPIPE_TYPE_BACKEND=auto  # Linux: auto|wayland|x11|wtype|xdotool|none; macOS: auto|osascript|none; Windows: auto|sendinput|none

# Transcript triggers (prefix-based; checked after transcription)
# Format: trigger=action pairs (comma-separated). If set but empty, disables triggers.
# Actions: strip (remove the trigger word), zwingli (LLM preprocessing)
# VOICEPIPE_TRANSCRIPT_TRIGGERS=zwingli=strip,zwingly=strip

# Zwingli LLM preprocessing (action=zwingli)
# VOICEPIPE_ZWINGLI_MODEL=gpt-4o-mini
# VOICEPIPE_ZWINGLI_TEMPERATURE=0.2
# VOICEPIPE_ZWINGLI_SYSTEM_PROMPT=You are a dictation preprocessor. Output only the final text to type.
# VOICEPIPE_ZWINGLI_USER_PROMPT=
# VOICEPIPE_ZWINGLI_BASE_URL=
# VOICEPIPE_ZWINGLI_API_KEY=  # defaults to OPENAI_API_KEY when unset

# Audio device index (optional)
# Use 12 for PulseAudio/PipeWire on most systems
VOICEPIPE_DEVICE=12
